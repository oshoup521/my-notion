# Production Environment Variables
PORT=8000
DATABASE_URL=sqlite:///./app.db

# Optional: For PostgreSQL database (Render provides free PostgreSQL)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name